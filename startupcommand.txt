# Install FFmpeg
echo "Installing FFmpeg..."
apt-get update -y
apt-get install -y ffmpeg
echo "FFmpeg installed. Starting Gunicorn..."

# Start the Flask app using Gunicorn
gunicorn --bind 0.0.0.0 --timeout 600 app:app